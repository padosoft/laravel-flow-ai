name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main, 'task/**']

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    name: PHP ${{ matrix.php }} / Laravel 13

    strategy:
      fail-fast: false
      matrix:
        php: ['8.3', '8.4', '8.5']

    steps:
      - name: Checkout laravel-flow-ai
        uses: actions/checkout@v4
        with:
          path: laravel-flow-ai

      # padosoft/laravel-flow has no tagged release yet, so composer.json
      # resolves it via a local path repository ("../padosoft-laravel-flow"
      # relative to this package's root) — mirror that layout here by
      # checking core out as a TRUE sibling directory, one level up from
      # laravel-flow-ai, matching what a local dev checkout looks like.
      #
      # ref is core's main — Macro D (the last macro to touch core) is
      # already merged there and Macro F has no active core-side macro
      # branch, so composer.json's "dev-main" constraint matches this ref
      # exactly. If a future F-PR needs an unreleased core change on a
      # dedicated core branch, retarget BOTH this ref and composer.json's
      # constraint together (see docs/LESSON.md's "cross-repo dev-branch
      # pinning" entry in the core repo for why this is a third location
      # that's easy to forget).
      - name: Checkout padosoft/laravel-flow (path-repo sibling)
        uses: actions/checkout@v4
        with:
          repository: padosoft/laravel-flow
          ref: main
          path: padosoft-laravel-flow

      - name: Setup PHP ${{ matrix.php }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: mbstring, openssl, pdo, tokenizer, xml, ctype, json, intl
          coverage: none
          tools: composer:v2

      - name: Validate composer.json
        working-directory: laravel-flow-ai
        run: composer validate --strict --no-check-publish

      - name: Install dependencies
        working-directory: laravel-flow-ai
        run: composer update --prefer-dist --no-interaction --no-progress

      - name: Run Pint (style check)
        working-directory: laravel-flow-ai
        run: composer format:test

      - name: Run PHPStan
        working-directory: laravel-flow-ai
        run: composer analyse

      - name: Run PHPUnit suites
        working-directory: laravel-flow-ai
        run: composer test
